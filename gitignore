# Generated CSV outputs (regenerated by running the scripts).
# figures/*.png and results/*.tex are deliberately committed so they can be
# inspected without running anything — see README, "Reproduced figures and tables".
results/*.csv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# GAMSPy / GAMS scratch files
*.gdx
*.lst
*.log
_gams_py_*
225*/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
